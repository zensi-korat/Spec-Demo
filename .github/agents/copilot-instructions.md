# Spec-Demo Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-01-07

## Active Technologies
- IndexedDB (via idb-keyval) for albums, photos, album order, and user preferences; no backend services (001-photo-organizer)

- TypeScript 5.x (strict mode enabled) with React 18.2+ + react-beautiful-dnd (drag-drop), date-fns (date formatting), idb-keyval (IndexedDB storage), lucide-react (icons) (001-photo-organizer)

## Project Structure

```text
src/
tests/
```

## Commands

npm test && npm run lint

## Code Style

TypeScript 5.x (strict mode enabled) with React 18.2+: Follow standard conventions

## Recent Changes
- 001-photo-organizer: Added TypeScript 5.x (strict mode enabled) with React 18.2+ + react-beautiful-dnd (drag-drop), date-fns (date formatting), idb-keyval (IndexedDB storage), lucide-react (icons)

- 001-photo-organizer: Added TypeScript 5.x (strict mode enabled) with React 18.2+ + react-beautiful-dnd (drag-drop), date-fns (date formatting), idb-keyval (IndexedDB storage), lucide-react (icons)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
